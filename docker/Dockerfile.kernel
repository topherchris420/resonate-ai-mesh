FROM rust:1.80-slim as builder
WORKDIR /app
COPY . .
RUN cargo build --release --bin kernel-core

FROM debian:bookworm-slim
WORKDIR /app
COPY --from=builder /app/target/release/kernel-core /app/
CMD ["/app/kernel-core"]
