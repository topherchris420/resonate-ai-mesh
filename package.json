{
  "name": "pordenone-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Unified Cognitive Cyber-Physical Command-and-Control Research Platform",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test && pytest services/ tests/ && cargo test --workspace",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "proto:generate": "bash scripts/generate-proto.sh"
  },
  "devDependencies": {
    "next": "^14.2.35",
    "turbo": "^2.0.0",
    "typescript": "^5.5.3"
  },
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=18"
  }
}
